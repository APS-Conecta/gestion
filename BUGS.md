# Bugs — APS Conecta Gestión

Fix log: what was wrong, and why the fix is right. **What is open right now lives in the issue
tracker** — [`label:bug`](https://github.com/APS-Conecta/gestion/issues?q=is%3Aissue+is%3Aopen+label%3Abug)
is the authority, and this file deliberately does not restate its count. It said "No bug is currently
open" until 2026-08-08, while three were: a sentence that can only ever be right by accident is the
always-green failure B-001 is in this very table for.

B-001…B-007 all came out of the **first end-to-end bring-up on a clean machine (2026-07-24)** — the repo
was v1 feature-complete on paper but had never been run start to finish. B-008…B-012 came out of Epic 5
and the hardening pass that followed. B-013 came out of looking at the screens that only appear when
something is wrong — the one class no routine check ever renders.

Each row names the cause and where the fix lives. The reasoning that made a fix non-obvious is a comment
beside the code it protects, not here — a second copy desyncs the day it is written.

| # | Symptom | Cause | Fix |
|---|---|---|---|
| B-001 | `make seed` reported success when a phase failed | `if ! ( set -e; … )` — bash suppresses `errexit` inside a command used as an `if` condition, and the suppression reaches into the subshell | [#39](https://github.com/APS-Conecta/gestion/issues/39) — run the subshell as its own command and test `$?`. Gated in `scripts/test.sh` |
| B-002 | `apps/`, `themes/` read-only for the host developer | `chown www-data:www-data` gave the container write access but left the host side `r-x` | [#40](https://github.com/APS-Conecta/gestion/issues/40) — chown to `www-data:$(HOST_GID)` plus `g+w` (`fix-mount-perms`) |
| B-003 | `ensure_app` blamed file permissions for every failure | both `occ` calls discarded stderr, then the helper asserted a cause it never checked | [#41](https://github.com/APS-Conecta/gestion/issues/41) — surface `occ`'s own error |
| B-004 | unquoted `NEXTCLOUD_TRUSTED_DOMAINS` broke `.env` | the value was sourced by the shell, so a space ran `nextcloud` as a command | [#42](https://github.com/APS-Conecta/gestion/issues/42) — quote it. `.env` is now parsed rather than sourced (`scripts/env.sh`) |
| B-005 | `make office-eurooffice` printed `OFFICE_JWT_SECRET` | the recipe line was echoed, and `occ` echoed it back | [#43](https://github.com/APS-Conecta/gestion/issues/43) — `@` the line, silence `occ`, print a neutral confirmation |
| B-006 | Xdebug log never writable; a warning on every request | `/tmp` is world-writable and Xdebug refuses to open a log it does not own there | [#44](https://github.com/APS-Conecta/gestion/issues/44) — later simplified away: no log file at all (`dev/xdebug.ini`) |
| B-007 | ODF opened read-only | the connector declares ODF `lossy-edit`/`auto-convert`, not `edit` | [#45](https://github.com/APS-Conecta/gestion/issues/45) — owner decision: lossy ODF editing on, via `editFormats`/`defFormats` in `14-office.sh` |
| B-008 | "Nextcloud" leaked into two UI surfaces after white-labeling | both strings are owned by upstream code, not by the theme, and the vendor block has no config lever without a paid subscription | `b66c5ac` — `server.css` hides `.section.development-notice`; the second surface accepted as admin-only. Both gated in `scripts/test.sh` |
| B-009 | `default_language=es_419` inert; the browser chose the UI language | `es_419` is a valid ICU **locale** but not a **language**, so `languageExists()` rejects it | `default_language=es` plus `force_language=es` (`10-locale.sh`). AD-7 corrected |
| B-010 | the eurooffice patches turned admin › Overview permanently red | patched files invalidate the `appinfo/signature.json` a store app ships | [#71](https://github.com/APS-Conecta/gestion/issues/71) — `12-apps.sh` drops the signature it just invalidated. Gated in `scripts/smoke.sh` |
| B-011 | the brand lockups rendered Georgia, not Fraunces | an SVG served as an image is an isolated document and cannot reach `server.css`'s `@font-face` | `themes/apsconecta/tools/embed-fonts.py` embeds a per-lockup subset as a `data:` URL, and the OS fallbacks are removed so a miss fails visibly. Gated in `scripts/test.sh` |
| B-012 | the header rules leaked onto public share pages; phase 30 discarded its own logs | `#header:not(.header-guest) #nextcloud` also matched `layout.public.php`, where that id is a `<div>` with content; and `ensure_groupfolder … >/dev/null` swallowed the lines `seed-idempotent.sh` greps for, so the gate could not fail on any group folder | `a36b458` — scope on `a#nextcloud`; drop the unused id `printf` and the redirect. Both gated in `scripts/test.sh` |
| B-014 | a certificate re-import would have reported the seed idempotent | `ensure_aia_intermediate` logs `certs: imported …`, and no alternative in `seed-idempotent.sh`'s `WRITES` matched that verb — so the one write in `provisioning/` outside the vocabulary was invisible to the gate. The meta-gate added in #126 only checks the other direction (every alternative still matches some log line) and says so at `test.sh:50-52` | `^ +certs: imported ` added to `WRITES`, **anchored**: the noop line for the same helper reads `certs: <name> already imported`, so an unanchored ` imported` would match it and redden every second seed. Both halves gated in `scripts/test.sh` |
| B-013 | eight screens — maintenance, the two upgrade screens, 429, the fatal exception, untrusted domain, the config error and the three setup screens — rendered in stock Nextcloud blue with Nextcloud's own logo | they render through the legacy `Template::printPage()`, which dispatches no `BeforeTemplateRenderedEvent`, and that event is the only thing that adds `server.css`; on an untrusted host `ThemingDefaults` is bypassed too, so even the identity strings came from a raw `\OC_Defaults` | [ADR-0004](docs/adr/0004-branding-the-legacy-render-path.md) — `themes/apsconecta/core/css/guest.css`, `themes/apsconecta/defaults.php`, a two-key `core/l10n/es.json` and the three icon files. Gated in both `scripts/test.sh` (enumeration of the 7 render sites) and `scripts/smoke.sh` (the screen itself) |
| B-015 | `make install` sat in phase 07-certs for minutes and only finished when the openssl PID was killed by hand | `ensure_aia_intermediate` bounded its second network call (`curl --max-time 20`) and not its first: closing `s_client`'s stdin only ends it AFTER a handshake returns, which does nothing for a peer that drops SYNs or accepts TCP and never speaks. Measured against `www.ispch.gob.cl`, which drops SYNs from some hosts — the stall is bounded only by `tcp_syn_retries` across every A record, ~4 min here, and reads as a hang | `timeout 20` in front of `openssl s_client`, inside the container payload |
| B-016 | a clinic whose DEIS row carries a quote or a backtick could not be provisioned, or was provisioned with no name at all | `scripts/deis.py` interpolated five register fields into `sites/<slug>/site.sh` inside double quotes, unescaped, and `seed.sh` SOURCES that file. Six values in the shipped register carry one. DEIS 113314's address holds a backtick — a syntax error that kills the phase loop before any phase runs. DEIS 201079's name holds quotes — parses clean, runs `Juan` as a command and leaves `SITE_NOMBRE` EMPTY, which is the silent half | `shlex.quote` on the five register fields and the three operator-typed emissions; not on `codigo`/`tipo`, which are digits and a nine-word enum. Gated in `scripts/test.sh` behaviourally over the WHOLE register — every row emitted, sourced by a real bash and compared byte-for-byte with the CSV, because `--snapshot` regenerates it and tomorrow's hostile value is one nobody has seen |
| B-017 | the gate that refuses to provision with this repository's published secrets could be disarmed by quoting a placeholder | `scripts/env.sh` held two dotenv readers that disagreed: the loader strips one layer of quotes and undoes compose's `$$` before exporting, while `require_real_secrets` read `.env.example` raw and compared the untransformed template value with the transformed exported one. Both transforms are conventions `.env.example` already uses, so a placeholder gaining either would leave `still` empty and a CESFAM installed with a published secret. It failed OPEN, and silently | the marker is what defines a placeholder, so test the parsed value for it directly rather than comparing bytes — which deletes the second reader instead of teaching it to agree. Gated in `scripts/test.sh` with the case that separates the two predicates: a value carrying the marker without equalling the template |
| B-018 | every seed "downgraded" the vendored apps to their pinned versions, and the next seed found them newer again — so `make seed-idempotent` could never converge | `ensure_vendored_app` unpacks the pinned tarball and then runs `occ upgrade` to reconcile `installed_version`. That upgrade re-downloads every ENABLED app from the app store (`Updater.php:244`), overwriting the bytes just unpacked. Measured: unpack `calendar-6.5.2.tar.gz` → `info.xml` reads 6.5.2 → `occ upgrade` → 6.5.3. So a VENDOR version and sha256 verified bytes that were then discarded, and ADR-0002's "the seed does not update apps" was false. `lib.sh` said `occ upgrade` "never contacts the store", and said the store stayed enabled because the admin Update button was "itself a guard (#82)" — #82 says the opposite and left the question open | [#163](https://github.com/APS-Conecta/gestion/issues/163) — `NC_appstoreenabled: "0"` on both the `nextcloud` and `cron` services in `compose.yaml`. Env and not a phase: the image's `/entrypoint.sh` runs `occ upgrade` on any image bump, before a seed can run, and a phase writing this key would rewrite it on every seed and redden the very gate this fixes. Gated as smoke check 13, which asserts the flag itself rather than the drift it prevents — a disk-vs-VENDOR comparison passes on a clean boot with the fix reverted |

| B-019 | opening any document from a machine that is not this box failed, and reported a **token / security** problem — so the search went to `OFFICE_JWT_SECRET`, where nothing was wrong | `DocumentServerUrl` is the URL the **browser** loads the editor from, and it defaults to `http://localhost:$OFFICE_PORT/`. That is correct while the browser IS on the box and wrong the moment it is not: `localhost:9980` then means the *laptop's* loopback, where nothing listens. The editor reports a token failure because that is what it says when it cannot come up at all, which points at the one thing that was already right — all three secrets (connector `jwt_secret`, documentserver `JWT_SECRET`, `.env`) hashed identically. The two container-to-container legs were 200 throughout, so nothing in the stack looked broken. Second, compounding cause: once Nextcloud is served over https, an `http://` editor URL is blocked as mixed content, so even a reachable address fails if the scheme does not match | `OFFICE_PUBLIC_URL` in `.env` — the lever `14-office.sh` already had (*"Set it when something proxies them outward, so the seed converges instead of clobbering"*), applied through `make seed` so it survives a reseed rather than being hand-set. **No code changed; the repo was right and the posture had moved.** `.env.example` now names the symptom, both rules (reachable from the browser, scheme must match) and the diagnosis order, because the failure mode is what misdirects. **Reachability cannot be gated from here** — `make office-smoke` runs on the box, where `localhost:9980` answers 200, so it passes on precisely the broken configuration; a green office gate says the server is healthy, never that a remote person can open a document. The **contradiction** can be, and is: smoke check 14 fails when a loopback `DocumentServerUrl` coexists with a non-loopback trusted domain (this instance is reached from somewhere that URL cannot serve), and when `overwriteprotocol=https` meets an `http://` editor URL (mixed content). Both arms must hold to fail, so the local-only posture this repo ships stays green — verified by simulating a shipped clinic, and each arm verified by seeding its violation |

The four rows below came out of the **slice-17 UI de-risk** (the provisionador's screens, 2026-09-22) — design-time finds, each already fixed in the locked fence the implementation assembles; recorded here per the house rule that a find lands in the ledger as it happens, not when it recurs.

| B-020 | the stub oracle's `docker ps` arm answered the estado leg's tab-template (`name\tstatus`) to every `ps` caller — install.sh/smoke/14-office's detection idiom greps `-qx` (whole-line) and the tab-polluted line broke the AIO arm inside the slice-16 checks | one verb, two `--format` shapes: the detection idiom asks for bare names, the new estado leg for name+status, and the stub could not tell them apart | the arm branches on a **substring test** (`any("{{.Status}}" in a for a in args)`) — the tab template arrives as ONE argv — the bare-name shape stays whole-line-exact for `grep -qx`; measured in the slice-17 self-test (estado + the s16 detection checks both green) |
| B-021 | the first cut of that substring test was written `"{{.Status}}" in args` — an **element** test — and never matched, because the tab template rides the argv as a single element | python's `in` on a list is membership, on a string a substring: the two shapes are spelled identically and differ only by the target's type | `any("{{.Status}}" in a for a in args)` — substring-per-element; the exact class B-020's row names, ledgered separately because the two mistakes look for different things when debugging |
| B-022 | the planilla screen's success path built `'<p><button id="paso6" onclick="location.href=…">…</button></p>'` as a quoted `onclick` inside a built string — a **JS syntax error** the substring greps were structurally blind to | a quoted onclick inside a single-quoted JS string with nested quotes is the R2-caught silent-green class: every marker grep passes, the browser dies | the handler is ATTACHED, never inlined (`document.getElementById("paso6").onclick = …`) — screen_cascada's own pattern; gated by `node --check` over every rendered script block (the JS syntax gate, 15 blocks/8 screens) — the missing gate R2 named |
| B-023 | the divergencia screen's `split("\n")` rode the Python f-string as a **raw newline**, not the two-character escape — node red on a literal line break inside a string literal | the same silent-green class as B-022 from the other side: the Python source, the rendered JS and the plan fence carry three different views of the same bytes | `split("\\n")` in the Python string (the JS text carries the escape); caught by the same `node --check` gate one fix after B-022 — the gate's own live proof |
| B-024 | territorio: the dev instance's browser gate was red on sector-save since 2026-09-15 with no app-level exception | two stacked causes: the floating `nextcloud:34-apache` image drifted (instance upgrade-pending, then a dispatcher that binds body params onto controller args by name) while `payload()` had always PUT the draft's `id` in the body — an int body `id` vs the route's `string $id` is a TypeError the dispatcher answers with a 400 logged only at debug | [territorio#138](https://github.com/APS-Conecta/territorio/pull/138) — `payload()` strips the `id` before the wire; `occ upgrade` unstuck the instance. Gate green end to end; 276 JS + 387 integration tests pass |
| B-025 | this box's `.env` never carried `NC_CONTAINER`, so every env-wrapped `occ`/`divergence` call exec'd against the AIO default container name — `make divergence` answered "cannot see the instance" while the stack was healthy | env.sh:41's default belongs to the AIO layout; the compose stack (S10 migration pending) needs the override the env.sh comment already prescribes but nothing enforces | `.env` gained `NC_CONTAINER=apsconecta-gestion-nextcloud-1` (local file, backup in /root/backups); surfaced by the intravox promotion's seed — a candidate .env.example key so the next box starts honest |
| B-026 | `make seed-idempotent` FAILs on `desktop_workspace/01-settings-getlogpath.patch — no longer applies` on the first re-seed after the chain converged | not patch rot: later patches legitimately DELETE (09) or rework (07) earlier patches' targets, so the earlier patch's reverse-check against the FINAL tree can neither apply nor reverse | FIXED in `apply_patch`: reverse-check runs FIRST (a seed is a convergence verb), then CHAIN PROOF — a later patch touching one of this patch's targets whose own reverse dry-run succeeds proves the chain ran past it — then the B-026 fallback (later patch references the target) — and only then forward. Gate green end to end |
| B-027 | `js/desktop-shell.js` accumulated THREE test seams — 06-test-pins re-applied on every seed that got past 01 | GNU patch fuzzy-matches 06's weak tail context onto 07's split file (forward branch ran before the already-applied check), and the tree had drifted undetected (an older-wording seam from a pre-reword chain) | fixed by B-026's reverse-first + chain proof (06 is now proven applied via 07/08); tree repaired by re-imposition (rm vendored dir + seed): one seam, 07's own wording — `desktop_workspace pins` green in test.sh |
| B-028 | `provisionador --self-test` dies at the ejecutar arm: the embedded seed's captured output carries no `== provisioning complete` banner (ValueError at provisionador.py:2116) | the stub docker's `app:enable` fell through stateless, so phase 41's enable-guard (`config:app:get intravox enabled` → nothing ≠ yes) FATAL'd inside the sandbox; the crash site then ran `.index()` on the banner BEFORE checking st, trading that FATAL for a bare ValueError | FIXED in the stub + self-test: the stub records the appconfig row `app:enable` really writes (upsert), answers `intravox:setup` by creating the 'IntraVox' folder row phase 41 looks up (query-first), and prints `groupfolders:list --output=json` in real occ's LIST shape — phase 41's row-iterating parser AttributeError'd silently against the old dict (measured live: `[{"id":1,...}]`); `seed_died()` now aborts at both banner sites WITH the seed's own output (this row's original ask). `provisionador --self-test`: 100 checks OK (was 64 + traceback); `make test` green end to end |

## Reporting a new one

Add a row. Put the reasoning that makes the fix non-obvious in a comment beside the code, and the gate
that stops it recurring in `scripts/test.sh` or `scripts/smoke.sh`.

```
| B-0NN | <symptom> | <cause> | <commit or issue> |
```
