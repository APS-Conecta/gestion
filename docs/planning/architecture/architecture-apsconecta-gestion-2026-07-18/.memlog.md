---
scope: APS Conecta Gestión v1 architecture spine — Foundation (Epic 0) + initial Spine A (documents) on white-label Nextcloud 33
purpose: Build substrate for the 3 devs to implement Epic 0 + Spine A; resolve deferred mechanism decisions (RBAC modeling, Collabora deployment, es-CL locale)
altitude: initiative (v1) — fixes invariants the Epic-0 + Spine-A features/stories must share
updated: 2026-07-19T13:01
---

- (constraint by prd) White-label ONLY, never a Nextcloud source fork (PRD NFR-5). Customization confined to apps/ (custom_apps), themes/, and config; Xdebug = derived dev-only image.
- (constraint by prd) OSS-first, self-hosted, zero paid licenses (PRD NFR-3); copyleft cores (NC AGPL, Collabora MPL, Paperless GPL later) accepted for self-hosting.
- (constraint by prd) Portability: core compose runs on any dev machine cross-OS; host.docker.internal works; nothing VPS-specific/absolute-path-bound (PRD NFR-1).
- (constraint by prd) Language split: UI Spanish es-CL, code/backend/identifiers English (PRD NFR-4). Data governance: no patient data, synthetic fixtures only (PRD NFR-2).
- (version by reality) Stack boot-checked ADOPTED: nextcloud:33-apache (NC 33.0.6) + postgres:16-alpine (PG16) + Redis come up clean; occ status installed. Pins to be re-verified on the web at finalize.
- (question by prd) Deferred mechanism decisions to resolve (PRD addendum): (1) RBAC category modeling — flat groups vs nested vs naming convention; (2) Collabora deployment — bundled CODE app vs separate Collabora Online service; (3) es-CL locale — discrete es-CL vs es/es_419 fallback.
- (direction by user) Working mode = Coaching (research + show alternatives per load-bearing call; user decides).
- (direction by user) Deliverables = ARCHITECTURE-SPINE.md (build substrate) + docs/ARCHITECTURE.md (canonical technical doc: stack, architecture, data flows). Write docs/ARCHITECTURE.md NEUTRALLY — do NOT frame it around 'the 3 devs' as audience; just the technical information.
- (version by research) NC33 CURRENT + supported ~monthly to ~Feb 2027 (~12mo/major). Latest patch 33.0.6 (25 Jun 2026, = boot-checked head). Pin options: 33-apache (rolling, auto-picks patches) vs 33.0.6-apache (byte-reproducible, manual bumps). NC34 exists but 33 = longest-soaked runway for greenfield. Sources: NC release wiki, endoflife.date.
- (version by research) Collabora: built-in CODE app 'richdocumentscode' + 'richdocuments' = zero extra container, no proxy/SSL/WOPI wiring, works after occ app:enable, but in-PHP-process => <5 editors, NOT production. Apple-Silicon devs need 'richdocumentscode_arm64'. Separate 'collabora/code' container = prod-like/10-100 editors but adds service + reverse-proxy/SSL + WOPI allow-list (docker subnet) + host.docker.internal; enabling external auto-disables built-in.
- (version by research) RBAC = Group Folders app 'groupfolders' (aka Team folders), first-party NC33-tracked. Supports folder->multiple groups + per-group Advanced Permissions ACL (Read/Write/Create/Delete/Share, inherit/allow/deny, down to subfolder). NC groups are FLAT (no native nesting) => model each role AND each category as parallel groups, add users to both (occ group:adduser, reproducible). Caveats: ACL cost grows with rule count x depth (keep shallow); Activity stream may leak ACL-hidden filenames.
- (version by research) Locale: NO discrete es_CL UI translation (use es_419, Latin-Am Spanish); locale es_CL IS valid for date/number formatting independently. TZ America/Santiago = per-user browser auto (no core instance-wide display-tz). Scriptable in config.php/occ: default_language/default_locale (seed, user-overridable) vs force_language/force_locale (locked). Verify rendered dd-mm-yyyy (issue #22785).
- (version by research) White-label theming = occ theming:config (name[NOT productName]/slogan/url/primary_color/background_color/logo/favicon/background) + disable-user-theming true. Fully scriptable config-as-code, NO fork, NO themes/ dir needed, NO defaults.php/opcache trap. Images must be local files on server (bind-mount then occ). Custom themes/ only for CSS the Theming app can't express (YAGNI).
- (decision by user) Image pin = ROLLING nextcloud:33-apache (auto-picks 33.x patches; accept minor cross-dev drift).
- (decision by user) Locale = DEFAULT BOTH (unlocked): config default_language=es_419 + default_locale=es_CL as seeds; users/devs may change language & formatting freely (no force_*).
- (direction by user) OFFICE EDITING requirement RAISED + reconsider: v1 must support >20 SIMULTANEOUS editors across MULTIPLE documents concurrently, keep multiple docs open side-by-side (tabs), maximum integration + easy access, a separate document-server that natively opens/keeps files open. Must stay OSS + free + self-hosted + complete office suite. => built-in CODE (<5 editors) INSUFFICIENT. Clarified: cannot combine built-in + external (external disables built-in); EuroOffice is not a NC online-editor (desktop suite). Real contenders: Collabora Online CODE vs OnlyOffice Docs. Researching concurrency caps.
- (version by research) PREMISE CORRECTED: the 10-doc/20-user cap is ONLY in the built-in richdocumentscode app, NOT the standalone collabora/code server (Collabora core dev Meeks: 'effectively no limit in coolwsd binaries'). Standalone CODE = MPL-2.0, no license concurrency cap, resource-bound ~10 users/CPU-thread + ~50MB/user (~20-30 editors ~= 3-4 vCPU + ~3GB RAM). Integrates via richdocuments (Nextcloud Office) over WOPI = WHITEST-label (editor IS 'Nextcloud Office'). NC33-ready. Multiple docs side-by-side = independent WOPI sessions.
- (version by research) OnlyOffice Docs Community Edition 9.4 (2026-05-22) REMOVED the 20-connection cap ('completely removed'); AGPL-3.0; single-process refactor (dropped RabbitMQ/DB dep) = lighter than before but still heavier baseline than Collabora; shows OnlyOffice branding in editor; best OOXML fidelity; integrates via onlyoffice connector (JWT, not WOPI). MUST pin >=9.4 or the 20-cap returns. NC33-compatible.
- (version by research) Euro-Office = NEW online office server (GA v1.0 2026-06-09), AGPL-3.0 clean fork of OnlyOffice OSS, 'Nextcloud Office powered by Euro-Office'. BUT only offered on NC34+/Hub 26 (no NC33 support) + weeks-old v1.0 => NOT viable for this NC33 deploy now; revisit at NC34+.
- (version by research) Office-server compose gotchas (chosen Collabora path): richdocuments->WOPI->collabora/code; set Collabora aliasgroup1/server_name to NC host; set NC Collabora URL + WOPI allow-list (docker subnet); host.docker.internal for callbacks (Linux: extra_hosts host-gateway); Collabora refuses plain HTTP => local dev use --o:ssl.enable=false or self-signed+termination; keep VPS-specific config OUT (portability).
- (decision by user) OFFICE SERVER = Collabora Online CODE as standalone collabora/code container, integrated via Nextcloud Office (richdocuments) over WOPI; do NOT install built-in richdocumentscode (that's where the 10-doc/20-user cap lives). Chosen for >20-concurrent-free + whitest-label + NC33-ready. OnlyOffice CE 9.4+ was the runner-up (MS-Office fidelity) but loses on branding/weight.
- (decision by coach) PARADIGM = Vanilla platform, configuration-as-code, no fork. Nextcloud+PG+Redis own the runtime & all product data; repo contributes ONLY config, theming, group/folder/ACL provisioning. ZERO custom PHP in v1. Binds: how every unit customizes NC. Prevents: divergent forks / hand-clicked drift / business logic in core.
- (decision by coach) STATE MUTATION = all desired state (branding, locale, ~21 role groups + category/all-staff groups, folder tree, ACLs, synthetic fixtures) applied by ONE idempotent occ-based provisioning shell script in the repo (run by 'make seed'), the single writer of desired state; never hand-clicked; not a custom app. Binds: who mutates instance state. Prevents: two writers / irreproducible instances.
- (decision by coach) DATA OWNERSHIP = Nextcloud owns product content (data volume) + metadata (PostgreSQL) + cache/locks (Redis). Repo owns desired-state recipe (provisioning script) + config ONLY, never product data/secrets. Binds: where each datum lives. Prevents: product data in git, config drift.
- (decision by coach) RBAC = Group Folders app (groupfolders). Each of ~21 roles = a flat NC group; the 4 categories + an 'all-staff' group = parallel flat groups (users added to role AND category groups by the provisioning script, since NC groups don't nest). Per-group Advanced Permissions ACL, deny-by-refinement, kept shallow. Binds: how access is granted. Prevents: per-individual grants, nesting assumptions, deep ACL trees. Caveat logged: Activity-stream may leak ACL-hidden names.
- (decision by coach) THEMING = occ theming:config (name/logo/favicon/colors/slogan/url/background) + disable-user-theming=true for brand consistency; NO fork, NO themes/ dir, NO defaults.php. Binds: how white-label is applied. Prevents: fork/opcache-trap path. (disable-user-theming=true is a brand-consistency lock; can relax later if devs need per-user themes.)
- (decision by coach) STACK/TOPOLOGY (seed) = single docker-compose: nextcloud:33-apache + postgres:16-alpine + redis + collabora/code; bind mounts apps/ (custom_apps, empty in v1) + themes/ (empty, theming via occ); Xdebug = derived dev-only image (profile); Makefile targets (up/down/seed/smoke/test); .env.example (gitignored .env). Portability: host.docker.internal cross-OS, nothing VPS-specific.
- (decision by coach) ENVIRONMENTS/OPERATIONAL ENVELOPE = v1 covers LOCAL DEV per developer only (one compose). DEFERRED (named, not silent): staging/production hosting, TLS/reverse-proxy for Collabora, backups/RTO-RPO, sizing/HA, production observability (PRD NFR-6). v1 ops surface = occ status + smoke target.
- (constraint by coach) CUSTOM-CODE BOUNDARY: v1 has NO custom PHP app (Layer-2/REM deferred). When custom apps arrive they live in apps/->custom_apps, must not fork or patch core, and consume only OCP public APIs. Binds: where extension code goes + its dependency rule.
- (event by coach) Reviewer gate (lint + 4 parallel lenses): lint 0; rubric = strong/buildable (0 critical, 1 high, 2 med, 3 low); web-verify = PASS (stack current); adversarial = 8 HIGH/MED incompatibility pairs (real substance); reconcile = 4 gaps. All triaged & fixed. Reviews in reviews/.
- (change by coach) Spine hardened from gate: (1) Added GROUP REGISTRY (21 role IDs+slugs+role->category; all-staff; cat-*; parameterizable prog-*/sector- team groups) closing H1/H6/H7+rubric-high+reconcile-R1. (2) AD-2 idempotency-by-guard (query-before-create) + fixed phase order + structure-vs-fixtures ownership partition (H4/H8/H9). (3) AD-4 group-only principals, every user in all-staff (single encoding), per-area/program/unit/sector mount model, allow-refinement NO DENY rules to protect FR-11 union (H2/H3). (4) AD-5/AD-8 3 named WOPI URLs, container-container via compose service names, host.docker.internal host-container only (H5). (5) redis:8-alpine (AGPL OSS-restored) + Stack license column + Valkey BSD alt (rubric license gate). (6) Deferred pointers for License-outline (NFR-3) + Legal/Chile SSOT (reconcile-R2/R3). (7) FR-14 org-conventions -> Conventions row (reconcile-R4). (8) AD-5 cap wording, theming literals (disable-user-theming yes, primary_color/background_color), make up-vs-seed, [ADOPTED] tags. docs/ARCHITECTURE.md synced.
- (event by coach) spine finalized (status: final). ARCHITECTURE-SPINE.md + docs/ARCHITECTURE.md. Next BMad step = bmad-create-epics-and-stories (Epic 0 first).
- (decision by user) STACK PIVOT (post-finalize amendment): Nextcloud 33 -> 34 (nextcloud:34-apache, head 34.0.1, EOL ~June 2027) + PostgreSQL 16 -> 18 (postgres:18-alpine, NC-recommended). Supersedes the earlier 'NC33 = longest-soaked N-1' rationale; accepted because the office-suite comparison requires NC34 (Euro-Office is NC34-only). AD-5 amended: office editing now SWITCHABLE between Collabora CODE (richdocuments/WOPI) and Euro-Office (ghcr.io/euro-office/documentserver + eurooffice connector/JWT), both NC34+ standalone, uncapped; added AD-11 = exactly one office backend active at a time (compose profiles + connector toggle, no MIME conflict). Redis 8 unchanged. PG major bump needs a fresh data dir (dev synthetic data). Deferred: pick the office-suite winner after comparison, then narrow AD-5.
