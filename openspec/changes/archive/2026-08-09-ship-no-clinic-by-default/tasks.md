## 1. Preserve the pilot before anything is removed

- [x] 1.1 Copy `sites/los-castanos/` to a location outside the repo working root and confirm the
      copy is readable — this is the only copy of the pilot's team, folder and ACL matrix once it
      leaves git tracking.
- [x] 1.2 Record in `CHANGELOG.md` under the unreleased heading that `sites/los-castanos/site.sh` is
      removed from git and that any working copy must preserve its own site file before updating.

## 2. Stop tracking establishment configuration

- [x] 2.1 Add `sites/*/` and `themes/apsconecta/core/css/site.css` to `.gitignore` under one comment
      stating the rule once: an artifact whose content depends on which establishment is installed is
      install-local, like `.env`. Confirm the pattern leaves `sites/establecimientos-deis-*.csv`
      tracked.
- [x] 2.2 `git rm -r --cached sites/los-castanos` and delete the working copy (task 1.1 holds it).
- [x] 2.3 `git rm --cached themes/apsconecta/core/css/site.css`, leaving the file on disk — the next
      install rewrites it and nothing an operator authored lives in it.
- [x] 2.4 Verify with `git ls-files sites/ themes/` that only the register CSV and the static theme
      assets remain.

## 3. Remove the establishment's name from the product

- [x] 3.1 Teach the ADR-0001 asset guard in `scripts/test.sh` (lines 109–120) to skip
      `themes/apsconecta/core/css/site.css` **by exact path, never by pattern** — it is generated by
      phase 15 and legitimately absent before the first install, while the other six paths are static
      assets whose absence is the bug the guard exists for. State that distinction in the comment.
- [x] 3.2 Assert the narrowed guard still bites: temporarily remove a *static* themed asset, confirm
      `scripts/test.sh` fails, restore it. A guard that stopped failing is worse than no guard.
- [x] 3.3 Rewrite the header comment inside `themes/apsconecta/core/css/site.css` and the block
      comment above `SITE_CSS=` in `provisioning/phases/15-branding.sh`: both describe a committed
      copy that "holds the reference clinic". The replacement says the file is generated per install,
      is not tracked, and is skipped by name in the asset guard. Do **not** delete the sentence about
      `scripts/test.sh` — the gate genuinely depends on this file, and the earlier plan was wrong to
      call that claim stale.
- [x] 3.4 Rewrite the two `require_site()` failure messages in `scripts/env.sh` so neither offers
      `SITE=los-castanos`: the unset case points at `.env` and the register search, the missing-file
      case keeps the two `deis.py` commands it already prints.
- [x] 3.5 Rewrite the post-`make setup` next-step block in `scripts/env-init.sh` (the `SITE` branch)
      so the only offered path is searching the register and writing a site file.
- [x] 3.6 Grep the tree for `casta` and confirm the only remaining hits are the DEIS register rows
      and `docs/adr/0005-gestion-is-the-development-trunk.md`, which is deliberately left alone as a
      dated record (design.md § *Decisions*).

## 4. Rewrite the operator-facing copy

- [x] 4.1 `README.md` step 3: replace "Choose your CESFAM" and the shipped-reference paragraph with
      choosing a primary-care establishment from the register — `deis.py` search, then
      `deis.py <codigo> --new <slug>`, then `SITE=<slug>` in `.env`. State that no establishment
      ships and that `make install` stops here until one is chosen.
- [x] 4.2 `README.md` step 4: replace the sample output's `✓ CESFAM Los Castaños — 7 teams, 14 group
      folders, 29 grants` with a placeholder that names no real establishment.
- [x] 4.3 `README.md` § *Where things live*: rewrite the `sites/` row — one `<slug>/site.sh` per
      establishment, gitignored as install-local configuration, plus the tracked DEIS register they
      are picked from. Drop "`los-castanos` ships as the reference clinic".
- [x] 4.4 `provisioning/README.md` § *The clinic's own data*: replace the "**One clinic ships**"
      paragraph with the no-default rule, keeping the existing points that `seed.sh` sources the file
      once before the phase loop and that `make seed` refuses until `SITE` names one that exists.
      Keep the sentence about the clean-boot job writing its own — it is now the primary evidence
      that the from-nothing path works.
- [x] 4.5 Sweep `README.md`, `provisioning/README.md` and `docs/ARCHITECTURE.md` for "CESFAM" used
      where any APS establishment is meant. In `ARCHITECTURE.md` that is lines 95, 149 and 195
      ("the CESFAM's documents", "a named CESFAM"); line 118 is the shared role registry and stays,
      because 22 roles really are a CESFAM's positions (design.md § *Vocabulary*). Do not
      find-and-replace.
- [x] 4.6 `docs/CONVENTIONS.md` lines 20 and 55: this is the Spanish staff-facing document shipped
      verbatim into every instance as `LÉEME — Convenciones.md`, so a PSR or SAPU install currently
      reads text calling it a CESFAM. Use *establecimiento* where the type is not the point. Keep the
      register of Spanish domain terms otherwise untouched — this is a noun swap, not a rewrite.

## 5. Make the picker read as what it is

- [x] 5.1 `scripts/deis.py` docstring: change the `deis.py cesfam florida` example so it does not
      read as a type filter, and state that the register carries CESFAM, PSR, CECOSF, CGR, CGU,
      COSAM, SAPU, SAR and SUR. Verify no picker logic changes — the search already matches any term.
- [x] 5.2 Check the interactive prompt strings and the comments `write_site` emits into a generated
      site file for the same CESFAM-only implication, applying the boundary from 4.5.
- [x] 5.3 Confirm `deis.py sapu <comuna>` and `deis.py <non-CESFAM codigo> --new <slug>` both work,
      to back the spec's non-CESFAM scenarios with an observed result rather than a reading of the
      code.

## 6. Verify from a clean clone

- [x] 6.1 Run `scripts/test.sh` in the working tree and confirm the gate is green with no tracked
      `sites/*/site.sh` and no tracked `site.css` — specifically that the `git ls-files` subset check
      and the narrowed asset guard both still pass.
- [x] 6.2 Clone the branch into a scratch directory, run `make setup`, then `make install`, and
      confirm it stops with the "choose an establishment" message and starts nothing. **Set a
      different `HTTP_PORT` in the scratch `.env`, or bring the pilot stack down first** — a second
      stack binds the same host port and the collision would read as a bug in this change.
- [x] 6.3 Provision a **non-CESFAM** establishment from nothing with `deis.py --new` + `make install`.
      NOTE: this cannot be done in a parallel scratch clone — `compose.yaml` pins
      `name: apsconecta-gestion`, so every checkout shares one project, one set of containers and one
      set of volumes (design.md § *Risks*). Own the host's single stack for the run, or wipe with
      `docker compose down -v` first.
- [x] 6.4 Confirm `git status` shows no NEW modification after a full install: neither the site file
      nor the generated `site.css` is offered for commit. This is the spec's clean-tree requirement
      and the whole reason `site.css` stopped being tracked.
- [x] 6.5 Load the running instance and confirm the establishment's name renders — proof that
      untracking `site.css` did not break the branding path, which no static check can show.

## 7. Restore the pilot and close

- [x] 7.1 Restore the preserved `sites/los-castanos/` into the working tree, confirm `git status`
      ignores it, and run `make install` to confirm the live suite converges unchanged — and that the
      tree is still clean afterwards, with the pilot's name present only in ignored files.
- [x] 7.2 Re-read the rendered `README.md` on the branch as a newcomer would, and confirm no
      establishment is named anywhere a reader could take as the default.
