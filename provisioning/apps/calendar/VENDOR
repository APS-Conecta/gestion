# Where the tarball beside this file came from (#98). Update all three lines together on a bump,
# then re-run `make seed`.
#
# 19 MB, the second largest thing in this repo after maps, and git keeps a FULL COPY per bump
# because a gzip stream does not delta-compress. Calendar releases often. If the repo ever has to
# go on a diet, this file and maps' are where the weight is — the alternative is contacting the
# app store at install time, which #98 decided against.
version=6.5.4
sha256=67c777a6ea974409e9d228c23fc60f2be8e675d376531b6b1b125f2f0365738d
url=https://github.com/nextcloud-releases/calendar/releases/download/v6.5.4/calendar-v6.5.4.tar.gz
