# Where the tarball beside this file came from (#98). Update all three lines together on a bump,
# then re-run `make seed`: the patches either still apply or the phase says so and stops.
version=6.0.1
sha256=445a70c6b955fa2dbea8925f2d3916fc28bc92deb493c4bf88a9905ca2494705
url=https://gitnet.fr/deblan/side_menu/releases/download/v6.0.1/side_menu_v6.0.1.tar.gz
#
# PINNED AT 6.0.1 DELIBERATELY — the url above is unreachable and has been since at least
# 2026-08-13. gitnet.fr is deblan's self-hosted forge and the app's ONLY source: no GitHub mirror,
# no app-store tarball, nothing on archive.org. It resolves (90.100.102.191) and then blackholes the
# SYN on both 80 and 443 — `curl --max-time 10` returns http=000 with time_connect=0.000000, which
# is a firewall dropping packets, not a service that is down. Re-measured 2026-09-14, unchanged.
#
# So the `frozen=` line below tells `make apps-check` to report this app without failing on it:
# 6.1.0 exists upstream and cannot be fetched by anyone from here, so a red line here could never be
# acted on. It is still listed on every run, with the version it cannot take, because a freeze that
# stops being visible is how a pin becomes a fossil. Nothing is broken — the 6.0.1 tarball beside
# this file is committed, sha-pinned and installs — but there is no route to a newer version until
# either the forge returns or someone mirrors it.
#
# If it comes back: bump all three lines together, DELETE the `frozen=` line, and re-run `make seed`.
# If it does not, the choices are to keep 6.0.1 indefinitely (today's decision), mirror the tarball
# somewhere we control, or drop the app. Decided in #169.
frozen=gitnet.fr, the app's only source, has been unreachable since 2026-08-13 (#169)
