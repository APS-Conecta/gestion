# Phase 10 — branding + es-CL locale.  OWNER: Epic 1 (only Epic 1 edits this file).
# Fill with `occ theming:config` (APS Conecta name/logo/colors) + es-CL language/format defaults,
# using config_system_set / config_app_set from lib.sh (idempotent). No-op stub until then.
phase_begin "10-branding" "APS Conecta branding + es-CL locale (Epic 1)"
: # TODO(Epic 1)
phase_end
