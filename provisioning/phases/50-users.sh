# Phase 50 — sample users (FIXTURE, dev-only — gated by SEED_FIXTURES).  OWNER: Story 0.6.
# Fill with ensure_user for a small set of synthetic staff, placed into the already-existing
# all-staff / role-* groups via add_user_to_group. Structure is owned by phases 20–40, not here.
phase_begin "50-users" "Synthetic sample users into existing groups (Story 0.6)"
: # TODO(Story 0.6)
phase_end
