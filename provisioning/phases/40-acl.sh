# Phase 40 — ACLs.  OWNER: Epic 3 (only Epic 3 edits this file).
# Fill with the first-cut access matrix as allow-refinement rules (NO DENY rules), binding the
# all-staff / role-* / team groups from phase 20 onto the phase-30 folders. No-op stub until then.
phase_begin "40-acl" "First-cut access matrix — allow-refinement ACLs (Epic 3)"
: # TODO(Epic 3)
phase_end
