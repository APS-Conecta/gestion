# Phase 60 — sample content (FIXTURE, dev-only — gated by SEED_FIXTURES).  OWNER: Story 0.6.
# Fill with deterministic synthetic sample documents/content placed into the phase-30 folders,
# so a fresh dev instance has something to look at. No real/clinical data, ever. No-op stub until then.
phase_begin "60-fixtures" "Deterministic synthetic sample content (Story 0.6)"
: # TODO(Story 0.6)
phase_end
