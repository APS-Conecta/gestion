# Phase 20 — groups.  OWNER: Epic 2 (only Epic 2 edits this file).
# Fill with the canonical registry via ensure_group: 21 role-* groups, cat-* categories,
# all-staff (every user), and parameterizable prog-* / sector-* team groups. No-op stub until then.
phase_begin "20-groups" "Role / category / team group registry (Epic 2)"
: # TODO(Epic 2)
phase_end
